import './styles.css'

export const Header = () => {
    return (
        <header>
            <h1>Albums - with ReactJS + Axios + JSON Placeholder </h1>
            <hr />
        </header>
    );
};